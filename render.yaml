services:
  - type: web
    name: interviewace-backend
    env: node
    plan: free
    buildCommand: npm install --legacy-peer-deps
    startCommand: node server.js
    envVars:
      - key: API_KEY
        sync: false
